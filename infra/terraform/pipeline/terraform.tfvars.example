# このファイルを terraform.tfvars（または環境別の命名）としてコピーし、環境に合わせて値を調整してください。

# AWS アカウント情報
aws_region      = "ap-northeast-1"
aws_account_id  = "000000000000" # 実際のアカウント ID はローカル terraform.tfvars にのみ記載してください

# パイプラインで利用する S3 バケット
site_bucket_name     = "developer-showcase-static-site"
artifact_bucket_name = "developer-showcase-artifacts"

# CodePipeline の GitHub リポジトリ設定
github_owner  = "your-github-account"
github_repo   = "developer-showcase"
github_branch = "main"

# GitHub 連携用 AWS CodeStar Connections
connection_arn = "arn:aws:codestar-connections:ap-northeast-1:000000000000:connection/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx" # 実値はローカルで管理

# CloudFront 設定
cloudfront_price_class    = "PriceClass_All"
cloudfront_waf_acl_arn    = ""

# （同一 tfvars を共用する場合の）Budgets 通知メール設定
# notification_emails = ["replace-with-alert-email@example.com"]
